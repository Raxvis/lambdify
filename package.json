{
	"author": {
		"email": "prefinem@gmail.com",
		"name": "Prefinem",
		"url": "https://prefinem.com"
	},
	"devDependencies": {
		"babel-core": "^6.26.3",
		"babel-eslint": "^8.2.2",
		"babel-jest": "^23.0.0",
		"babel-plugin-transform-es2015-modules-commonjs": "^6.26.0",
		"babel-plugin-transform-object-rest-spread": "^6.26.0",
		"babel-preset-env": "^1.7.0",
		"eslint": "^4.19.0",
		"eslint-config-prettier": "^2.9.0",
		"gulp": "^3.9.1",
		"gulp-babel": "^7.0.0",
		"gulp-eslint": "^4.0.2",
		"gulp-eslint-if-fixed": "^1.0.0",
		"gulp-if": "^2.0.2",
		"gulp-sourcemaps": "^2.6.4",
		"husky": "^0.14.3",
		"jest": "^23.0.0",
		"jest-cli": "^23.0.0",
		"lerna": "^2.8.0",
		"pkg-ok": "^2.1.0",
		"prettier": "1.13.2",
		"pretty-quick": "^1.6.0",
		"regenerator-runtime": "^0.11.1",
		"through2": "^2.0.3"
	},
	"license": "MIT",
	"name": "lambdify-mono-repo",
	"scripts": {
		"build": "gulp build",
		"clean": "yarn lint && yarn prettify",
		"compile": "lerna bootstrap && gulp build && lerna bootstrap",
		"lint": "eslint packages/**/src/*.js --fix",
		"precommit": "yarn lint && pretty-quick --staged && yarn test",
		"prepublishOnly": "yarn clean && yarn compile && yarn test && pkg-ok",
		"prettify": "prettier --config ./.prettierrc.json --write \"packages/**/src/*.js\"",
		"start": "lerna bootstrap && gulp watch",
		"test": "jest --coverage",
		"watch": "learn run watch"
	}
}
