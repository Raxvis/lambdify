{
    "author": {
        "email": "prefinem@gmail.com",
        "name": "Prefinem",
        "url": "https://prefinem.com"
    },
    "devDependencies": {
        "babel-core": "^6.26.0",
        "babel-eslint": "^8.2.2",
        "babel-jest": "^23.0.0",
        "babel-plugin-transform-es2015-modules-commonjs": "^6.26.0",
        "babel-plugin-transform-object-rest-spread": "^6.26.0",
        "babel-preset-env": "^1.7.0",
        "eslint": "^4.19.0",
        "husky": "^0.14.3",
        "jest": "^23.0.0",
        "jest-cli": "^23.0.0",
        "lerna": "^2.8.0",
        "pkg-ok": "^2.1.0",
        "regenerator-runtime": "^0.11.1",
        "through2": "^2.0.3"
    },
    "license": "MIT",
    "name": "lambdify-mono-repo",
    "scripts": {
        "build": "lerna run build",
        "clean": "lerna run clean",
        "precommit": "lerna bootstrap && lerna run build && lerna bootstrap && lerna run clean && jest",
        "prepublishOnly": "lerna bootstrap && lerna run build && lerna bootstrap && lerna run clean && jest && pkg-ok",
        "start": "lerna bootstrap && lerna run build && lerna bootstrap",
        "test": "jest --coverage",
        "watch": "learn run watch"
    }
}